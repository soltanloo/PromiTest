export const ThrowBypassSystemPrompt = "You will receive an Javascript async function. Reply 'T' if the error throwing of this function can be bypassed only by changing the arguments of the function. Reply 'F' if it cannot.";